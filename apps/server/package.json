{
  "name": "server",
  "scripts": {
    "dev": "pnpm run --hot src/index.ts",
    "start": "pnpm run src/index.ts",
    "build": "pnpm build ./src/index.ts --compile --outfile server"
  },
  "dependencies": {
    "@prisma/client": "^6.8.2",
    "@prisma/extension-accelerate": "^1.3.0",
    "better-auth": "^1.2.8",
    "hono": "^4.7.10",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
