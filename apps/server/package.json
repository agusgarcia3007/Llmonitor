{
  "name": "server",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "bun run src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build ./src/index.ts --compile --outfile server",
    "postinstall": "bun prisma generate --no-engine"
  },
  "dependencies": {
    "@prisma/client": "^6.8.2",
    "@prisma/extension-accelerate": "^1.3.0",
    "better-auth": "^1.2.8",
    "hono": "^4.7.10",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prisma": "^6.8.2"
  },
  "engines": {
    "bun": "1.1.4"
  },
  "packageManager": "pnpm@10.11.0"
}
